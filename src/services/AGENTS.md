# services

Kylin-specific services for commit chart panels.

- **Commit.ts** – `getAllPerAuthor`, `getCurrentFileCommitHistory`; uses execFile for git
- **Configuration.ts** – `getCommitChartConfiguration` (commitChart.* settings)
- **Output.ts** – Output channel "Simple GIT Stats"
