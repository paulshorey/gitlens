# scripts

Build-time Node scripts (not shipped with extension).

- **generateLicenses.js** – Fetches license text, generates ThirdPartyNotices
- **generateEmojiShortcodeMap.js** – Builds emoji shortcode map from gitmojis.json
- **applyPatchForInsiders.js** – Patches package.json for insiders build
